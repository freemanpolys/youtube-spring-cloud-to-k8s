#spring:
#  cloud:
#    config:
#      discovery:
#        enabled: true
#        service-id: cloud-config
#      fail-fast: true
spring:
  cloud:
    kubernetes:
      config:
        name: application-config